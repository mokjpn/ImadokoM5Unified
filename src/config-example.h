// Machine and environment settings

const char* ssid = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";

// MQTT settings
const char* mqtt_server = "YOUR_MQTT_HOSTNAME";
const int mqtt_port = YOUR_MQTT_PORT;
const char* mqtt_user = "YOUR_MQTT_USERNAME";
const char* mqtt_password = "YOUR_MQTT_PASSWORD";
const char* mqtt_topic = "owntracks/YOUR_MQTT_USERNAME/YOUR_DEVICE_ID";

// staticmap settings
const char* staticmap_url = "http://YOUR_WEB_SERVER/staticmap.php";

// image buffer size
#define BUFFERSIZE 100000
